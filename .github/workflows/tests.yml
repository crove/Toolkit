name: Tests

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install Poetry
        run: |
          pip install poetry

      - name: Install dependencies
        run: |
          cd cli-taskmanager
          poetry install

      - name: Run tests
        run: |
          cd cli-taskmanager
          poetry run pytest -v src/cli_taskmanager/tests
